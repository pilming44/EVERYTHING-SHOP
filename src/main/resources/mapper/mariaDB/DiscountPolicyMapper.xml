<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="maria.DiscountPolicyDAO">
    <select id="selectDiscountRate" parameterType="String" resultType="Integer">
        /* DiscountPolicyDAO.selectDiscountRate 현재시점의 할인율 조회*/
        SELECT T01.DISCOUNT_RATE
          FROM T_DISCOUNT_POLICY T01
         WHERE T01.GRADE_CD = #{gradeCd}
           AND SYSDATE() BETWEEN T01.START_DATE AND T01.END_DATE
    </select>
</mapper>